<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ismart.androidui">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.TabLayoutActivity" />
        <activity android:name=".test.ImageChooseActivity" />
        <activity android:name=".test.MDDialogActivity" />
        <activity android:name=".test.OKHttpActivity" />
        <activity android:name=".test.ViewActivity" />
        <activity android:name=".test.ListActivity" />
        <activity android:name=".test.BannerActivity" />
    </application>

</manifest>